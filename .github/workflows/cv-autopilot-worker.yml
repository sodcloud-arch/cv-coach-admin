name: CV Coach Autopilot Worker (PAUSED)

# AUTOPILOT PAUSED BY OWNER DECISION.
# Keep this workflow as a reversible control point, but do not run autonomously.
# Restoring the previous workflow from Git history re-enables the worker when desired.

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cv-coach-autopilot
  cancel-in-progress: false

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Autopilot is paused
        run: |
          echo "CV Coach Autopilot is paused."
          echo "No autonomous worker, external executor, self-review, or OpenAI API call is executed by this workflow."
