name: CV Coach Autopilot Worker — DISABLED

# Autopilot intentionally disabled by coach request on 2026-09-13.
# No scheduled, push, issue, comment, or pull-request event can execute it.
# Kept as an inert manual workflow so the previous configuration remains easy to restore.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  autopilot-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "CV Coach Autopilot is disabled."
